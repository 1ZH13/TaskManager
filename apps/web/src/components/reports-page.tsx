'use client';
/* eslint-disable react-hooks/set-state-in-effect -- repository reads occur after client mount. */

import { useCallback, useEffect, useMemo, useState } from 'react';
import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { selectWorkReport, type ReportFilters, type WorkReport } from '@task-manager/domain';
import type { Board, BoardColumn, Person, Project, ProviderReference, Team, WorkActivity, WorkItem } from '@task-manager/shared';
import type { RepositoryError } from '@task-manager/data';
import { Button } from '../../../../packages/ui/src/index';
import { useDemo } from './demo-context';

const labels: Record<string, string> = { TODO: 'Pendientes', IN_PROGRESS: 'En progreso', BLOCKED: 'Estancadas', DONE: 'Terminadas', TASK: 'Tareas', RECURRING_TASK: 'Recurrentes', INCIDENT: 'Incidencias', SUBTASK: 'Subtareas', MILESTONE: 'Hitos', LOW: 'Baja', MEDIUM: 'Media', HIGH: 'Alta', URGENT: 'Urgente', UNASSIGNED: 'Sin asignar' };
const formatNumber = new Intl.NumberFormat('es-PA', { maximumFractionDigits: 1 });
const panamaDay = () => {
  const parts = new Intl.DateTimeFormat('en-US', { timeZone: 'America/Panama', year: 'numeric', month: '2-digit', day: '2-digit' }).formatToParts(new Date());
  const value = (type: string) => parts.find(part => part.type === type)?.value ?? '';
  return `${value('year')}-${value('month')}-${value('day')}`;
};

function Distribution({ title, values, labelFor = (key: string) => labels[key] ?? key }: { title: string; values: WorkReport['byStatus']; labelFor?: (key: string) => string }) {
  return <section className="report-distribution"><h2>{title}</h2>{values.length === 0 ? <p>Sin datos para los filtros actuales.</p> : <table><thead><tr><th scope="col">Categoría</th><th scope="col">Tareas</th></tr></thead><tbody>{values.map(({ key, value }) => <tr key={key}><th scope="row">{labelFor(key)}</th><td>{value}</td></tr>)}</tbody></table>}</section>;
}
function ChartCard({ title, values, labelFor }: { title: string; values: WorkReport['byStatus']; labelFor?: (key: string) => string }) {
  const resolve = labelFor ?? ((key: string) => labels[key] ?? key); const data = values.map(point => ({ ...point, label: resolve(point.key) }));
  return <section className="report-chart"><h2>{title}</h2>{data.length === 0 ? <p>Sin datos para los filtros actuales.</p> : <><div className="report-chart__canvas" role="img" aria-label={`${title}: ${data.map(point => `${point.label}, ${point.value}`).join('; ')}`}><ResponsiveContainer width="100%" height="100%"><BarChart data={data} margin={{ top: 8, right: 8, bottom: 8, left: -16 }}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="label" tick={{ fontSize: 12 }} interval={0}/><YAxis allowDecimals={false}/><Tooltip/><Bar dataKey="value" fill="#4583BD" radius={[4, 4, 0, 0]}/></BarChart></ResponsiveContainer></div><Distribution title={`Tabla de ${title.toLocaleLowerCase('es-PA')}`} values={values} labelFor={resolve}/></>}</section>;
}

export function ReportsPage({ projectId: forcedProjectId }: { projectId?: string }) {
  const { phId, actor, repository } = useDemo();
  const [projects, setProjects] = useState<Project[]>([]); const [boards, setBoards] = useState<Board[]>([]); const [people, setPeople] = useState<Person[]>([]); const [teams, setTeams] = useState<Team[]>([]); const [providers, setProviders] = useState<ProviderReference[]>([]); const [items, setItems] = useState<WorkItem[]>([]); const [columns, setColumns] = useState<BoardColumn[]>([]); const [activities, setActivities] = useState<WorkActivity[]>([]); const [filters, setFilters] = useState<Omit<ReportFilters, 'phId'>>({ projectId: forcedProjectId }); const [error, setError] = useState<RepositoryError | null>(null); const [loading, setLoading] = useState(true);
  const load = useCallback(() => { setLoading(true); setError(null); void repository.listProjects({ phId, includeArchived: false }).then(async nextProjects => { const visibleProjects = (actor.role === 'ADMIN' ? nextProjects : nextProjects.filter(project => actor.projectIds.includes(project.id))).filter(project => !forcedProjectId || project.id === forcedProjectId); const [page, nextActivities, nextBoards, nextPeople, nextTeams, nextProviders] = await Promise.all([repository.listWorkItems({ phId }), repository.listActivity(phId), Promise.all(visibleProjects.map(project => repository.listBoards({ phId, projectId: project.id }))).then(groups => groups.flat()), repository.listPeople({ phId }), repository.listTeams({ phId }), repository.listProviders({ phId })]); const nextColumns = await Promise.all(nextBoards.map(board => repository.listBoardColumns({ phId, boardId: board.id }))); setProjects(visibleProjects); setBoards(nextBoards); setPeople(nextPeople); setTeams(nextTeams); setProviders(nextProviders); setItems(page.items.filter(item => visibleProjects.some(project => project.id === item.projectId))); setActivities(nextActivities); setColumns(nextColumns.flat()); }).catch(setError).finally(() => setLoading(false)); }, [actor.projectIds, actor.role, forcedProjectId, phId, repository]);
  useEffect(load, [load]);
  const report = useMemo(() => selectWorkReport({ items, columns, projects, activities }, { phId, ...filters }, panamaDay()), [activities, columns, filters, items, phId, projects]);
  const personLabel = (key: string) => key === 'UNASSIGNED' ? labels[key] : people.find(person => person.id === key)?.displayName ?? key;
  const teamLabel = (key: string) => key === 'UNASSIGNED' ? labels[key] : teams.find(team => team.id === key)?.name ?? key;
  return <section className="workspace-page reports-page"><div className="toolbar"><div><p className="eyebrow">Informes</p><h1>Estado del trabajo</h1><p>Indicadores calculados directamente desde tareas y actividad de la PH actual.</p></div><Button variant="secondary" onClick={load}>Actualizar</Button></div><form className="report-filters" onSubmit={event => event.preventDefault()}><label>Módulo<select value={filters.module ?? ''} disabled={!!forcedProjectId} onChange={event => setFilters(current => ({ ...current, module: event.target.value as Project['module'] || undefined, projectId: forcedProjectId }))}><option value="">Todos</option><option value="ADMINISTRATIVE">Administrativa</option><option value="OPERATIONS">Operaciones</option><option value="ACCOUNTING">Contabilidad</option></select></label><label>Proyecto<select value={filters.projectId ?? ''} disabled={!!forcedProjectId} onChange={event => setFilters(current => ({ ...current, projectId: event.target.value || undefined, boardId: undefined }))}><option value="">Todos</option>{projects.filter(project => !filters.module || project.module === filters.module).map(project => <option key={project.id} value={project.id}>{project.key} · {project.name}</option>)}</select></label><label>Tablero<select value={filters.boardId ?? ''} onChange={event => setFilters(current => ({ ...current, boardId: event.target.value || undefined }))}><option value="">Todos</option>{boards.filter(board => !filters.projectId || board.projectId === filters.projectId).map(board => <option key={board.id} value={board.id}>{board.name}</option>)}</select></label><label>Equipo<select value={filters.teamId ?? ''} onChange={event => setFilters(current => ({ ...current, teamId: event.target.value || undefined }))}><option value="">Todos</option>{teams.map(team => <option key={team.id} value={team.id}>{team.name}</option>)}</select></label><label>Persona<select value={filters.assigneeId ?? ''} onChange={event => setFilters(current => ({ ...current, assigneeId: event.target.value || undefined }))}><option value="">Todas</option>{people.map(person => <option key={person.id} value={person.id}>{person.displayName}</option>)}</select></label><label>Proveedor<select value={filters.providerId ?? ''} onChange={event => setFilters(current => ({ ...current, providerId: event.target.value || undefined }))}><option value="">Todos</option>{providers.map(provider => <option key={provider.id} value={provider.id}>{provider.name}</option>)}</select></label><label>Desde<input type="date" value={filters.from ?? ''} onChange={event => setFilters(current => ({ ...current, from: event.target.value || undefined }))}/></label><label>Hasta<input type="date" value={filters.to ?? ''} onChange={event => setFilters(current => ({ ...current, to: event.target.value || undefined }))}/></label></form>{error ? <section className="tm-state tm-state--error" role="alert"><p>{error.message}</p><Button variant="secondary" onClick={load}>Reintentar</Button></section> : loading ? <div className="tm-skeleton" aria-label="Cargando informes"/> : report.total === 0 ? <section className="tm-state" aria-live="polite"><h2>Sin tareas para estos filtros</h2><p>Cambia o restablece los filtros para consultar otro conjunto de trabajo.</p></section> : <><div className="metric-grid" aria-label="Indicadores"><Metric label="Pendientes" value={report.pending}/><Metric label="Terminadas últimos 7 días" value={report.completedLastSevenDays}/><Metric label="Próximas a vencer" value={report.dueSoon}/><Metric label="Atrasadas" value={report.overdue}/><Metric label="Estancadas" value={report.blocked}/><Metric label="Tiempo medio de resolución" value={report.averageResolutionHours === null ? '—' : `${formatNumber.format(report.averageResolutionHours)} h`}/><Metric label="Cumplimiento" value={report.completionRate === null ? '—' : `${formatNumber.format(report.completionRate * 100)}%`}/></div><div className="report-grid"><ChartCard title="Tareas por estado" values={report.byStatus}/><ChartCard title="Tareas por tipo" values={report.byType}/><ChartCard title="Tareas por prioridad" values={report.byPriority}/><ChartCard title="Carga pendiente por persona" values={report.workloadByAssignee} labelFor={personLabel}/><Distribution title="Por equipo" values={report.byTeam} labelFor={teamLabel}/><Distribution title="Por responsable" values={report.byAssignee} labelFor={personLabel}/><Distribution title="Cumplimientos por fecha" values={report.completionByPeriod}/></div></>}</section>;
}
function Metric({ label, value }: { label: string; value: string | number }) { return <article className="metric-card"><h2>{label}</h2><p>{value}</p></article>; }
